## 八、当前系统视角小结（2026-03-24）

### 1. 系统定位

JD-data 当前定位仍然不变：

- **数据仓库**
- **特征工厂**
- **标签工厂**
- **baseline / walk-forward 验证底座**

当前阶段仍属于：

- **训练与验证底座收口期**

而不是：

- 主计算引擎开发期
- 复杂模型扩张期
- 主引擎仓切换期

现阶段最高优先级不是“把模型做复杂”，而是先把下面这件事查清楚并收口：

**为什么当前模型在正式 walk-forward 验证中没有赢过弱基线，且部分指标显著更差。**

---

### 2. 当前已经完成的链路

截至 2026-03-24，以下链路已经形成首版闭环：

#### 基本面链

- `data_fundamental/02618.HK/fundamental_statement_items.csv`  
  已形成 2021–2025 首版底表

- `data_fundamental/02618.HK/fundamental_features.csv`  
  已形成首版可消费特征层

#### 模型数据链

- `data_model/02618.HK/model_features.csv`  
  已重建为可运行版本，当前为 `model_features_v2.2`

- `scripts/build_model_features.py`  
  已修复为完整可运行版本

- 已移除首轮确认应剔除的代理层特征：
  - `ret_1519_*`
  - `alpha_vs_1519_5d`

- `data_model/02618.HK/model_labels.csv`  
  已生成

- `data_model/02618.HK/model_dataset.csv`  
  已形成首版训练/验证数据集

#### 验证链

以下 workflow 已跑通并产生产物：

- `Walk-forward eval v1`
- `Generate walk-forward report v1`
- `Feature diagnostics v1`

对应正式产物已存在：

- `data_model/02618.HK/walk_forward_eval_v1.json`
- `data_model/02618.HK/walk_forward_analysis_v1.json`
- `reports/walk_forward_analysis_v1.md`
- `data_model/02618.HK/feature_diagnostics_v1.json`
- `reports/feature_diagnostics_v1.md`

---

### 3. 当前阶段最重要的阶段性结论

当前系统已经不是“能不能产出数据”的问题，而是进入了一个更关键的阶段：

**数据链能跑通，但验证结果证明当前特征集质量还不足以支撑模型稳定胜过弱基线。**

也就是说：

- 数据工程首版已经基本成立
- 训练/验证链已经基本成立
- 但当前特征集仍有噪声、缺失与弱信号混杂的问题
- 系统当前瓶颈，已经从“能不能生成数据”切换成“生成的数据有没有真实预测价值”

这是当前 JD-data 主线的真正位置判断。

---

### 4. 当前正式分析结论（必须记住）

#### 加权整体指标结论

当前正式 analysis 产物显示：

##### 回归
- MAE：模型显著更差
- RMSE：模型显著更差
- IC：证据不足以证明更优

##### 分类
- Accuracy：证据不足以证明更优
- F1：证据不足以证明更优
- ROC AUC：证据不足以证明更优
- Brier：模型显著更差

#### 阶段判断

当前没有任何指标可以判定为“显著更优”。

因此，当前不能进入：

- 复杂模型扩张
- 主引擎仓开发
- 提前做高级分布预测叠层

否则会把“底层信噪比不够”的问题掩盖掉。

---

### 5. diagnostics 当前给出的核心信号

当前 diagnostics 已经比上一轮更干净：

- 不再出现已剔除的 `ret_1519_*`
- 不再出现 `alpha_vs_1519_5d`

说明：

**特征集与 diagnostics 产物的一致性已经基本恢复。**

但 diagnostics 同时也明确暴露出当前仍需继续处理的问题：

#### 高缺失优先关注特征
- `external_isc_customer_yoy`
- `external_isc_arpc_yoy`
- `receivables_loss_allowance_ratio`
- `contract_assets`
- `contract_assets_ratio`

#### 当前相关性表现并不强
无论对 `y_ret_1d` 还是 `y_up_1d`，目前 top |corr| 也仍然偏弱，说明：

- 有效信号仍然不够集中
- 高缺失特征可能在拖累训练
- 当前特征层仍需继续做“减法”和清洗，而不是立刻做“加法”

---

### 6. 当前主线判断

当前正确主线不是：

- 回退去讨论 old issue
- 重新讨论 annual 是否补齐
- 再去问 workflow 能不能跑
- 直接切去复杂模型
- 直接切主引擎仓

当前正确主线是：

**继续做“特征修剪收口”，先把训练样本的信噪比抬起来。**

并且当前优先级判断如下：

#### 第一优先级
继续特征修剪

#### 第二优先级
待修剪后重新跑：
- eval
- analysis
- diagnostics

#### 第三优先级
只有当结果至少收敛到“证据不足以证明更差”后，才考虑：
- 特征增强
- horizon 分层

#### 暂不进入
- 复杂模型
- 主引擎仓
- 高复杂度策略层

---

## 九、下次对话直接执行口令

下次进入这个仓库主线后，直接按下面口令继续：

**继续 JD-data 主线，不回退旧话题，严格按仓库内《交互契约.MD》执行，默认授权有效，不再重复确认，不再让我做方案选择。当前系统定位不变：JD-data 仍是数据仓库 + 特征工厂 + 标签工厂 + baseline / walk-forward 验证底座，当前阶段仍属于训练与验证底座收口期，不是主计算引擎开发期。当前正式结论已经明确：模型在 MAE、RMSE、Brier 上显著弱于弱基线，暂无任何指标可判定为显著更优。下一步不要切复杂模型，不切主引擎仓，直接继续“特征修剪收口”。优先处理高缺失且弱信号特征：`external_isc_customer_yoy`、`external_isc_arpc_yoy`、`receivables_loss_allowance_ratio`、`contract_assets`、`contract_assets_ratio`。修剪后，重新生成 `model_dataset.csv`，再重跑 `Walk-forward eval v1`、`Generate walk-forward report v1`、`Feature diagnostics v1`，并给出新的加权整体指标、Wilcoxon 显著性结论、top missing、top |corr| 结果，再判断下一步是继续修剪、进入特征增强，还是做 horizon 分层；但暂不进入复杂模型和主引擎仓。**

---

## 十、本文件用途

本文件是 2026-03-24 当前阶段的 JD-data 主线衔接锚点，作用是：

- 防止下次继续时回退到旧阶段
- 明确系统当前所处的是“训练与验证底座收口期”
- 明确当前不是主引擎开发阶段
- 明确数据链、标签链、验证链都已首版成型
- 明确当前真正矛盾已经从“能不能产出”切换成“为什么模型没有赢弱基线”
- 明确当前应继续做特征修剪，而不是提前复杂化
- 避免把时间浪费在重复查旧问题、重复验老问题、重复讨论已收口问题上

---

## 十一、一句话状态结论

**当前 JD-data 已完成基本面底表、特征层、标签层、训练数据集与 walk-forward 验证首版闭环；系统已经从“数据能不能出来”进入“当前特征集为什么仍显著弱于弱基线”的收口阶段。下一步主线不是扩张复杂度，而是继续特征修剪，提升训练样本信噪比，再用正式验证产物决定是否进入增强或 horizon 分层。**
