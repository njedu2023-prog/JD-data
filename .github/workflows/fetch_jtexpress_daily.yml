name: fetch jtexpress_daily

on:
  workflow_dispatch:
  schedule:
    - cron: "30 11 * * 1-5"

permissions:
  contents: write

jobs:
  fetch-jtexpress-daily:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
      TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas tushare

      - name: Run fetch_jtexpress_daily.py
        run: python scripts/fetch_jtexpress_daily.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            data_raw/1519.HK/hk_daily_raw.csv \
            data_clean/1519.HK/daily_clean.csv \
            refresh_log/refresh_log.csv

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "refresh 1519.HK raw clean"
            git push
          fi
