# 0321 衔接口令

## 一、当前主线位置

当前 JD-data 仓库主线，已经从“基本面首版落库”阶段，继续推进到了：

**PDF / 年报公告 → staging JSON → statement_items 入库 → fundamental_features 首版生成**

也就是说，主线已经不再是：

- 要不要建表
- 表头怎么设计
- ingestion 要不要做

这些抽象问题。

当前真实主线已经变成：

**`statement_items` 五年底表已完成首版落库，下一步进入 `statement_items -> fundamental_features` 映射补全阶段。**

---

## 二、本轮已经完成的核心成果

### 1. 基本面底座结构已从单表扩展为双层结构

当前已形成两层核心表：

#### A. 底层标准 item 表
`data_fundamental/02618.HK/fundamental_statement_items.csv`

定位：

- 标准化承接财报 / 年报 / 年度业绩公告中的细颗粒字段
- 一行 = 一个 item
- 用于沉淀可追溯、可回查、可扩展的底层财务与经营要素

主键规则已确认：

`symbol + report_date + period_type + statement_type + item_code`

#### B. 消费层特征表
`data_fundamental/02618.HK/fundamental_features.csv`

定位：

- 面向模型消费、解释层、标签层
- 一行 = 一个财报周期
- 是后续模型读取的上游特征表，不是最底层事实表

### 2. 相关脚本与 workflow 已落地

已落地并跑通过的关键文件包括：

#### 脚本
- `scripts/init_fundamental_consumer_tables.py`
- `scripts/ingest_statement_items_json.py`
- `scripts/build_fundamental_features.py`

#### workflow
- `.github/workflows/init_fundamental_consumer_tables.yml`
- `.github/workflows/ingest_statement_items_json.yml`
- `.github/workflows/build_fundamental_features.yml`

#### 支撑文档
- `《fundamental_statement_items.csv 表结构 V1 + item_code 清单》.MD`
- `《fundamental_features.csv 表结构 V1 + 派生规则 V1》.MD`

这意味着：

**消费层壳子、入库链、build 链都已经存在，且 workflow 可执行。**

---

## 三、本轮已完成的数据范围

### 1. statement_items 五年首版落库已完成

当前 `fundamental_statement_items.csv` 已完成以下年度的首版入库：

- `2021 FY`
- `2022 FY`
- `2023 FY`
- `2024 FY`
- `2025 FY`

也就是：

**2021–2025 五个年度的京东物流标准 item 底表已建立。**

### 2. 当前已成功写入的高价值 item 范围

已经进入 `statement_items` 的重点字段包括但不限于：

#### 资产负债表类
- `cash_and_cash_equivalents`
- `restricted_cash`
- `term_deposits`
- `trade_receivables`
- `contract_assets`
- `inventories`
- `trade_payables`
- `borrowings`
- `lease_liabilities`
- `current_assets`
- `current_liabilities`
- `total_assets`
- `total_liabilities`
- `total_equity`

#### 现金流类
- `operating_cash_flow`
- `investing_cash_flow`
- `financing_cash_flow`
- `free_cash_inflow`
- `capex_net`

#### 收入 / 客户结构类
- `revenue_jd_group`
- `revenue_external`
- `revenue_integrated_supply_chain`
- `revenue_external_integrated_supply_chain`
- `external_isc_customer_count`
- `external_isc_arpc`

#### 营运质量类
- `receivables_within_3m`
- `receivables_3_to_6m`
- `receivables_6_to_12m`
- `receivables_over_12m`
- `receivables_loss_allowance`
- `payables_within_3m`
- `payables_3_to_6m`
- `payables_6_to_12m`
- `payables_over_12m`
- `supplier_finance_arrangements`

注意：
不同年份可得字段略有差异，但整体五年骨架已经形成。

---

## 四、当前验收结论

### 1. `fundamental_statement_items.csv` 验收通过

已确认：

- 2021–2025 五年数据都已真实写入
- 主键层面未见明显冲突
- `source_doc / source_page / source_section / quality_flag / note` 等追溯信息存在
- 当前表已经具备“标准底表”价值

当前结论：

**statement_items 层已经首版完成。**

### 2. `fundamental_features.csv` 只能算 V1 首版通过

已确认：

- workflow 能执行
- 文件能更新
- 2021–2025 时间序列能生成
- annual / semiannual / quarter 行均存在

但是：

#### 当前核心问题不是 workflow，而是映射逻辑没打透

虽然 `fundamental_features.csv` 表头里已经预留了大量 statement_items 对应字段，例如：

- `cash_and_cash_equivalents`
- `restricted_cash`
- `term_deposits`
- `trade_receivables`
- `contract_assets`
- `trade_payables`
- `borrowings`
- `lease_liabilities`
- `current_assets`
- `current_liabilities`
- `investing_cash_flow`
- `financing_cash_flow`
- `free_cash_inflow`
- `capex_net`
- `revenue_jd_group`
- `revenue_external`
- `revenue_integrated_supply_chain`
- `revenue_external_integrated_supply_chain`
- `external_isc_customer_count`
- `external_isc_arpc`
- 应收应付账龄类字段
- `supplier_finance_arrangements`

但当前实际生成的 annual 行里，很多对应列仍然是空值。

也就是说：

**不是 build 没跑，而是 build 脚本还没有把这些 item 真正 merge 进 feature 表。**

### 3. 所以当前真实判断是

- `statement_items`：已完成首版落库
- `features`：首版框架与 build 链已跑通
- **真正未完成部分：`statement_items -> fundamental_features` 消费映射**

---

## 五、当前主线应该如何理解

### 当前已经完成的，不要再重复折腾：
- 不用再讨论 schema
- 不用再证明 workflow 能不能跑
- 不用再继续做 2021–2025 年份接入
- 不用再反复重跑 `build_fundamental_features.yml` 来验证“会不会生成文件”

因为这些都已经验证过了。

### 当前真正卡住的，不是流程而是逻辑：
**`scripts/build_fundamental_features.py` 还没有把 statement_items 里的字段真正消费到 feature 行里。**

---

## 六、下一次进入主线后的第一优先任务

下次进入这个仓库主线后，不再回退旧话题，不再讨论要不要继续接年份。

### 第一优先任务：
直接进入：

# `statement_items -> fundamental_features` 映射补全 V2

重点目标：

### A. 先补 annual 口径
优先补 2021 / 2022 / 2023 / 2024 / 2025 的 annual 行，把以下字段真正写入 `fundamental_features.csv`：

- `cash_and_cash_equivalents`
- `restricted_cash`
- `term_deposits`
- `trade_receivables`
- `contract_assets`
- `inventories`
- `trade_payables`
- `borrowings`
- `lease_liabilities`
- `current_assets`
- `current_liabilities`
- `investing_cash_flow`
- `financing_cash_flow`
- `free_cash_inflow`
- `capex_net`
- `revenue_jd_group`
- `revenue_external`
- `revenue_integrated_supply_chain`
- `revenue_external_integrated_supply_chain`
- `external_isc_customer_count`
- `external_isc_arpc`
- `receivables_within_3m`
- `receivables_3_to_6m`
- `receivables_6_to_12m`
- `receivables_over_12m`
- `receivables_loss_allowance`
- `payables_within_3m`
- `payables_3_to_6m`
- `payables_6_to_12m`
- `payables_over_12m`
- `supplier_finance_arrangements`

### B. 再让派生字段真正长出来
在字段接通后，重点派生：

- `net_cash`
- `working_capital`
- `debt_to_asset_ratio`
- `lease_burden_ratio`
- `receivables_ratio`
- `contract_assets_ratio`
- `inventory_ratio`
- `payables_ratio`
- `supplier_finance_ratio`
- `receivables_over_6m_ratio`
- `receivables_over_12m_ratio`
- `payables_over_12m_ratio`
- `receivables_loss_allowance_ratio`
- `free_cash_flow_margin`
- `capex_intensity`
- `operating_cash_flow_margin`
- `external_revenue_ratio`
- `jd_group_revenue_ratio`
- `integrated_supply_chain_revenue_ratio`
- `external_isc_revenue_ratio`

### C. 再统一 quality_flag 逻辑
当前 `quality_flag` 大多还是：

- `PARTIAL`
- `RAW`

下一阶段要根据字段到位率、来源质量、是否 narrative rounded 值，升级质量标记。

---

## 七、当前不该优先做的事

在下一阶段开始前，以下事情都不是第一优先：

### 1. 不优先整合 workflow
原因：
workflow 已经足够支撑当前主线，真正阻塞点不在 yml。

### 2. 不优先扩 2020
原因：
2020 不是 2618 上市公司年报口径，而要走 prospectus 历史财务路线，复杂度更高。
应在 2021–2025 mapping V2 做完后再开。

### 3. 不优先大改 README
原因：
当前主线更需要代码与映射落地，不是文档包装。

---

## 八、关于 2020 的正确定位

### 2020 不能按 2618 年报口径处理
因为京东物流 2618 是 2021 年上市，2020 没有上市公司年报。

### 如果后续要做 2020
应从：

**招股书 / prospectus 历史财务数据**

进入，而不是从 annual report 流程进入。

所以：

**2020 是后续独立支线，不是当前主线第一优先。**

---

## 九、下次对话直接执行口令

下次进入这个仓库主线后，直接按下面口令继续：

继续 JD-data 主线，不回退旧话题，不再重复检查 workflow 是否跑通，直接进入 `statement_items -> fundamental_features` 映射补全 V2。先以 2021–2025 五个 annual 行为第一优先，补齐 `cash_and_cash_equivalents`、`restricted_cash`、`term_deposits`、`trade_receivables`、`contract_assets`、`inventories`、`trade_payables`、`borrowings`、`lease_liabilities`、`current_assets`、`current_liabilities`、`investing_cash_flow`、`financing_cash_flow`、`free_cash_inflow`、`capex_net`、`revenue_jd_group`、`revenue_external`、`revenue_integrated_supply_chain`、`revenue_external_integrated_supply_chain`、`external_isc_customer_count`、`external_isc_arpc`、应收应付账龄和 `supplier_finance_arrangements` 等字段的消费映射；再继续生成 `net_cash`、`working_capital`、`debt_to_asset_ratio`、`lease_burden_ratio`、`receivables_ratio`、`supplier_finance_ratio`、`free_cash_flow_margin`、`capex_intensity`、`external_revenue_ratio`、`jd_group_revenue_ratio`、`integrated_supply_chain_revenue_ratio`、`external_isc_revenue_ratio` 等派生特征，并收敛 `quality_flag` 逻辑。

---

## 十、本文件用途

本文件是本轮 JD-data 基本面主线推进后的衔接锚点，作用是：

- 防止下次继续时回退旧阶段
- 明确 2021–2025 `statement_items` 五年底表已经建成
- 明确 `fundamental_features` 当前问题不在 workflow，而在消费映射逻辑
- 明确下次主线应直接进入映射补全 V2
- 避免把时间浪费在重复验 workflow、重复接年份、重复讨论 schema 上

---

## 十一、一句话状态结论

当前 JD-data 已完成 2021–2025 京东物流 `fundamental_statement_items.csv` 五年首版落库，`fundamental_features.csv` 的首版生成链也已跑通；当前真正未完成的不是 workflow，而是 `statement_items -> fundamental_features` 的消费映射尚未打透。下次主线应直接进入映射补全 V2，而不是回到旧话题。
