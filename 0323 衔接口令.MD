# 0323 衔接口令

## 一、当前主线位置

当前 JD-data 仓库主线，已经从“基本面首版落库 / statement_items → fundamental_features 映射补全”阶段，继续推进到了：

**全历史模型消费母表落地完成，主线正式切换到“标签层 + 训练闭环首版设计”阶段。**

也就是说，当前主线已经不再是：

- 要不要建表
- workflow 能不能跑
- annual 是否补齐
- `model_features.csv` 为什么只有 2025
- 历史交易日有没有被吃进去

这些问题都已经处理过了。

当前真实主线已经变成：

**以 `model_features.csv` 为首版正式训练母表，继续向 `model_labels.csv`、`model_dataset.csv`、基线训练与验证闭环推进。**

---

## 二、本轮已经完成的核心成果小结

### 1. 基本面底层与消费层双层结构已落地

当前已形成两层核心表：

#### A. 标准 item 底表
`data_fundamental/02618.HK/fundamental_statement_items.csv`

定位：

- 标准化承接财报 / 年报 / 年度业绩公告中的细颗粒字段
- 一行 = 一个 item
- 用于沉淀可追溯、可回查、可扩展的底层财务与经营要素

#### B. 消费层特征表
`data_fundamental/02618.HK/fundamental_features.csv`

定位：

- 面向模型消费、解释层、后续标签层
- 一行 = 一个财报周期
- 是后续模型读取的上游基本面特征表，不是最底层事实表

---

### 2. 基本面链路已从“壳子存在”推进到“可真实消费”

相关脚本和 workflow 已落地并跑通过：

#### 脚本
- `scripts/init_fundamental_consumer_tables.py`
- `scripts/ingest_statement_items_json.py`
- `scripts/build_fundamental_features.py`

#### workflow
- `.github/workflows/init_fundamental_consumer_tables.yml`
- `.github/workflows/ingest_statement_items_json.yml`
- `.github/workflows/build_fundamental_features.yml`

说明：

- consumption 壳子已存在
- statement_items 入库链已存在
- fundamental_features build 链已存在
- 2021–2025 五年 annual 主干消费映射已推进完成首版可用状态

---

### 3. `model_features.csv` 已从“只到 2025”修复为“覆盖全历史交易日”

本轮最关键的新增收口成果：

#### 已确认事实
- `data_clean/02618.HK/daily_clean.csv` 有完整历史，最早到 **2021-05-28**
- 原先 `data_model/02618.HK/model_features.csv` 只有 **247 行**，起点是 **2025-01-02**
- 当前该问题已定位并修复，不再是悬而未决的问题

#### 已锁定的真实根因
根因不在源数据不足，而在：

**旧版 `scripts/build_model_features.py` 以 `hk_trade_calendar.csv` 为唯一主轴，导致历史被不完整 calendar 截断。**

#### 已完成的修复
`build_model_features.py` 已完成修正，核心口径改为：

- 不再让 `hk_trade_calendar.csv` 作为唯一硬主轴
- 改为以 **`02618.HK` 自身 `daily_clean.csv` 的实际交易日** 作为正式 asof_date 主轴
- calendar 仅保留为辅助诊断，不再负责截断样本母表

#### 修复后的结果
当前 `data_model/02618.HK/model_features.csv` 已经：

- 从 **2021-05-28** 开始
- 覆盖全部可用历史交易日
- 行数已扩展到与 `daily_clean.csv` 对齐的量级（约 **1184 行**）
- `build_version` 已升级到 **`model_features_v2.1`**

这意味着：

**模型消费母表“全历史覆盖”这个关键阻塞点已经收口。**

---

## 三、当前阶段的系统级判断

### 1. 数据底座阶段，已经基本完成
到当前为止，JD-data 已具备以下核心上游：

#### 市场层
- 个股：`02618.HK daily_clean.csv`
- 指数：HSI / HKTECH / HSCEI
- 代理层：9618 / 3690 / 9988 / 2057 / 1519

#### 基本面层
- `fundamental_statement_items.csv`
- `fundamental_features.csv`

#### 模型消费层
- `model_features.csv`

这说明系统已经不是“有没有数据”的问题，而是进入“如何定义预测任务并训练”的问题。

---

### 2. 当前主阻塞已经从“数据工程”切换到“训练闭环设计”
以前的主阻塞是：

- 基本面数据没入库
- 消费层字段没长出来
- model_features 只覆盖 2025

现在这些关键口子都已经打通到可用状态。

所以当前真正的主线阻塞，不再是工程接数，而是：

- 标签怎么定义
- 训练任务怎么切
- 数据集怎么合成
- 基线模型怎么跑
- 验证集与误差分析怎么做

---

## 四、当前不应该继续反复折腾的事

下列事项已经不是第一优先，不要再回退：

### 1. 不再反复检查 workflow 是否能跑
原因：

- 核心 workflow 已多轮验证
- 当前主线阻塞不在 yml，而在训练设计

### 2. 不再回头争论 annual 是否补齐
原因：

- 当前已足够支撑首版训练实验
- 后续可继续增量补强，但不应阻塞训练主线

### 3. 不再围绕 `model_features.csv` 的历史覆盖问题循环排障
原因：

- 问题已经定位
- 根因已经修复
- 结果已经体现到正式文件

### 4. 不优先扩 2020
原因：

- 2020 不属于 2618 上市后标准年报口径
- 若后续需要，应走 prospectus / historical financials 支线
- 当前不应干扰训练闭环首版主线

---

## 五、下一阶段主线应如何理解

当前系统已经从：

**“数据底座建设阶段”**

切换到：

**“首版模型训练闭环设计阶段”**

这句话要明确记住。

也就是说，下一阶段不再以“多建一个表、多补几个字段”为主要目标，而是要把现有数据真正推进成：

- 可训练
- 可验证
- 可回测
- 可解释
- 可迭代

的首版预测系统数据集。

---

## 六、下一次进入主线后的第一优先任务

下次进入这个仓库主线后，不再回退旧话题，不再讨论 `model_features` 背景，也不再重复做“历史是否全量覆盖”的排查。

### 第一优先任务：

**直接进入 `标签层 + 训练闭环 V1` 设计与落地。**

---

## 七、下一步计划（按顺序执行）

### A. 先定义标签层
新增首版标签脚本与标签表：

#### 目标脚本
`scripts/build_model_labels.py`

#### 目标产物
`data_model/02618.HK/model_labels.csv`

### 首版建议优先生成的标签字段
- `y_ret_1d`：T+1 收盘收益率
- `y_up_1d`：T+1 是否上涨（分类标签）
- `y_ret_5d`：T+5 收盘收益率
- `y_ret_20d`：T+20 收盘收益率

要求：

- 标签必须严格基于未来窗口生成
- 特征与标签严格按时间对齐
- 不允许任何未来信息泄漏进 `model_features`

---

### B. 再合成首版训练数据集
在标签表生成后，继续合成：

#### 目标产物
`data_model/02618.HK/model_dataset.csv`

#### 合成逻辑
按：
- `symbol`
- `asof_date`

将：
- `model_features.csv`
- `model_labels.csv`

合并成首版正式训练数据集。

---

### C. 再进入基线模型验证
在 `model_dataset.csv` 成型后，进入基线训练实验，不要一开始就复杂化。

首版建议优先验证：

#### 回归基线
- Linear Regression / Ridge

#### 分类基线
- Logistic Regression

#### 树模型基线
- LightGBM 或 XGBoost（二选一即可）

目标不是追求立刻高分，而是先验证：

- 数据链是否闭环
- 标签是否合理
- 特征是否有基本预测价值
- 不同类别特征的边际贡献是否存在

---

### D. 再做误差分析与特征价值判断
基线跑通后，再重点看：

- 哪些年份表现较好 / 较差
- 基本面层是否带来真实增益
- 市场层是否主导短期预测
- 代理层是否具有补充价值
- 哪些特征应保留、削弱、剔除
- 后续是否要做分布预测 / 分位预测

---

## 八、下次对话直接执行口令

下次进入这个仓库主线后，直接按下面口令继续：

**继续 JD-data 主线，不回退旧话题，不再重复解释 `model_features` 的背景，不再讨论 annual 是否补齐，也不再重复排查“为什么只有 2025 年”这个已解决问题。当前已确认 `model_features.csv` 已覆盖 `02618.HK` 全部可用历史交易日。下一步直接进入“标签层 + 训练闭环 V1”阶段：先设计并落地 `scripts/build_model_labels.py`，生成 `data_model/02618.HK/model_labels.csv`，首版至少包含 `y_ret_1d`、`y_up_1d`、`y_ret_5d`、`y_ret_20d`；再合成 `data_model/02618.HK/model_dataset.csv`；随后进入基线模型验证与误差分析。**

---

## 九、本文件用途

本文件是 2026-03-23 当前阶段的 JD-data 主线衔接锚点，作用是：

- 防止下次继续时回退到旧阶段
- 明确 statement_items / fundamental_features / model_features 三层链路已形成
- 明确 `model_features.csv` 全历史覆盖问题已经修复并收口
- 明确当前主线已从“数据工程”切换到“标签层 + 训练闭环设计”
- 避免把时间浪费在重复验 workflow、重复查历史覆盖、重复讨论 annual 补齐上

---

## 十、一句话状态结论

**当前 JD-data 已完成 2021–2025 基本面底表与消费层首版落地，并已将 `model_features.csv` 修复为覆盖 `02618.HK` 全部可用历史交易日；当前真正的下一阶段主线，不再是继续证明数据能不能产出，而是直接进入 `model_labels.csv`、`model_dataset.csv` 与基线训练验证构成的首版训练闭环。**
