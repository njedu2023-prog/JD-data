# 0319 衔接口令（更新版）

## 一、当前主线位置

当前仓库主线已从“日频行情原始数据 / 清洗数据接入”正式推进到 **京东物流基本面数据底座建设**。

截至本次收口，当前主线已经不再停留在 schema 讨论阶段，而是已经形成了 **可运行的基本面首版落库闭环**。

---

## 二、本轮已完成的核心成果

### 1. 基本面层定位已明确

JD-data 不再只有价格层：

- `data_raw`
- `data_clean`

而是新增：

- `data_fundamental`

用于为后续京东物流预测模型提供：

- 未来 1 个月预测支撑
- 未来 6 个月预测支撑
- 财报周期状态识别
- 事件后趋势判断
- 经营质量变化跟踪

### 2. 主表正式落地并持续入库

主表路径已落地并跑通：

- `data_fundamental/02618.HK/fundamental_quarterly.csv`

当前主表粒度已确认：

- 一行 = 一个财报 / 经营披露周期

兼容周期：

- `quarter`
- `semiannual`
- `annual`

主键规则已确认：

- `symbol + report_date + period_type`

### 3. 相关支撑文件与闭环能力已建立

当前已形成的基本面层首版文件包括：

- `data_fundamental/02618.HK/fundamental_quarterly.csv`
- `data_fundamental/02618.HK/fundamental_quarterly_dictionary.md`
- `data_fundamental/02618.HK/fundamental_source_mapping_v1.md`
- `data_fundamental/02618.HK/ingest_log.csv`
- `scripts/validate_fundamental_table.py`
- `.github/workflows/ingest_fundamental_manual.yml`

这意味着当前已经具备：

**喂数据 → 入库 / upsert → 校验 → 留痕 → 自动提交**

的首版闭环能力。

---

## 三、当前已完成的数据范围

### 1. 2021 年已完成两期入库

已入库：

- 2021 H1
- 2021 FY

### 2. 2022 年已完成四期入库

已入库：

- 2022 Q1
- 2022 H1
- 2022 Q3
- 2022 FY

### 3. 2023 年已完成四期入库

已入库：

- 2023 Q1
- 2023 H1
- 2023 Q3
- 2023 FY

### 4. 2024 年已完成四期入库

已入库：

- 2024 Q1
- 2024 H1
- 2024 Q3
- 2024 FY

### 5. 2025 年已完成四期入库

已入库：

- 2025 Q1
- 2025 H1
- 2025 Q3
- 2025 FY

当前主表已形成：

- 2021：2 条
- 2022：4 条
- 2023：4 条
- 2024：4 条
- 2025：4 条

即 **2021–2025 基本面首版骨架已形成**。

---

## 四、当前验收结论

### 1. `fundamental_quarterly.csv` 验收通过

已确认：

- 表头结构稳定
- 主键无重复
- 记录已真实写入
- 2021–2025 时间骨架已形成

注意：

当前 `quality_flag` 仍以 `REVIEW` 为主，这属于合理状态。  
原因是部分字段仍为管理层口径、摘要值、近似值或阶段性人工整理值，不应过早标记为完全稳定。

### 2. `ingest_log.csv` 验收通过

已确认：

- 有成功留痕
- 能区分插入与更新
- `upsert` 逻辑正常
- workflow 不是假成功

### 3. 基本面落库链路已跑通

当前已验证链路：

**原始公告 / 人工整理 → `temp_fundamental_records_XXXX.json` → `ingest_fundamental_manual` → `fundamental_quarterly.csv` / `ingest_log.csv`**

---

## 五、关于当前主表的关键判断

### 1. 当前这份 `fundamental_quarterly.csv` 后期模型可以直接吃吗？

结论：

**可以直接作为模型上游输入表，但不能作为最终训练表裸喂。**

也就是说：

- 可以直接读取
- 可以作为基本面源表
- 但不能不经处理就把全部字段原样喂给成熟模型

### 2. 当前这张表适合做什么

适合做：

- 基本面特征源表
- 中长期判断辅助层
- 模型解释层
- 财报后状态识别输入层

### 3. 当前不适合直接裸喂的原因

主要问题有四类：

#### （1）字段口径未完全统一

最明显的是：

- `external_customer_revenue`

不同年份中存在口径混用风险：

- 有时更像外部客户总收入
- 有时更像外部一体化供应链客户收入

#### （2）缺失值很多，且属于结构性缺失

尤其在：

- Q1 / Q3 资产负债表字段
- 现金流字段
- 周转类字段
- 估值类字段

这些缺失不是随机缺失，而是由公告披露口径决定。

#### （3）混合了标准报表字段与管理层摘要字段

例如：

- 标准字段：`total_assets`、`accounts_receivable`、`operating_cash_flow`
- 摘要字段：`warehouse_count`、`major_event_note`、`management_guidance`

#### （4）文本字段很多，不能原样喂数值模型

例如：

- `revenue_growth_quality_note`
- `profit_quality_note`
- `cash_flow_quality_note`
- `major_event_note`
- `management_guidance`
- `quality_note`

---

## 六、当前关于“企业基本面是否够用”的结论

### 1. 够不够支撑系统最高价值目标？

结论：

**够做第一版中长期基本面底座，但还不够作为成熟版标准底座。**

现在这张表已经具备价值，因为它已经覆盖：

- 收入
- 毛利 / 毛利率
- 净利 / 调整后净利
- 经营现金流
- 资产负债表核心项（主要在 H1 / FY）
- 一体化供应链客户收入
- 外部客户相关收入
- 客户数 / 仓网规模 / 重大事件
- 管理层口径说明与质量备注

这足以支撑：

- 未来 1 个月判断
- 未来 6 个月判断
- 财报后状态识别
- 基本面解释层

但还不够成熟，原因是：

- 口径仍有混用
- 标准化程度不够
- 派生因子层尚未建立
- 标准财报底表尚未独立建立

### 2. 那些空字段是否需要标准财务报表来补？

结论：

**需要，而且很有价值。**

尤其以下字段更依赖标准财务报表：

- `total_assets`
- `total_liabilities`
- `total_equity`
- `current_assets`
- `current_liabilities`
- `accounts_receivable`
- `accounts_payable`
- `inventory`
- `interest_bearing_debt`
- `operating_cash_flow`
- `investing_cash_flow`
- `financing_cash_flow`
- `cash_and_equivalents`
- `restricted_cash`

进一步还能推导出：

- `net_cash`
- `working_capital`
- `debt_to_asset_ratio`
- `inventory_turnover_days`
- `accounts_receivable_turnover_days`
- `accounts_payable_turnover_days`
- `free_cash_flow`
- `capex`（统一口径）

### 3. 下载 6 年标准财务报表有没有价值？

结论：

**有，而且价值很大。**

价值不只是“补空格”，更在于：

- 统一字段口径
- 提高可比性
- 生成可训练的派生财务因子
- 降低人工摘要值的歧义
- 为自动化抽取建立更稳底座

---

## 七、当前 ingestion 脚本能力的真实判断

### 1. 现在脚本能不能直接吃 Excel / Word / PDF 并写入主表？

结论：

**不能直接原生吃下。当前稳定吃的是 JSON，不是原始文档。**

当前已经跑通的是：

**原始资料 → 人工/半人工整理成 `temp_fundamental_records_xxxx.json` → `ingest_fundamental_manual` → `fundamental_quarterly.csv`**

也就是说，目前脚本的核心能力是：

- 吃 JSON
- 校验字段
- upsert 写入主表
- 写入 `ingest_log.csv`

而不是：

- 直接解析 PDF
- 直接解析 Word
- 直接解析 Excel
- 自动识别表格字段并入库

### 2. 未来能不能扩成支持 Excel / Word / PDF？

结论：

**能，而且值得做。**

推荐未来采用三层 ingestion 架构：

#### A. 原始文件层

- `raw_docs/*.pdf`
- `raw_docs/*.docx`
- `raw_docs/*.xlsx`

#### B. 抽取中间层

- `extracted_text/*.md`
- `parsed_tables/*.csv`
- `staging/*.json`

#### C. 正式入库层

- `staging/*.json` → `fundamental_quarterly.csv`

即：

**不推翻现有 ingestion，而是在前面增加“文档转 staging JSON”的预处理层。**

---

## 八、下次进入主线后的正确开发方向

下次进入开发，不再回到 schema 抽象讨论，也不再回退到旧话题。  
当前主线已经不再是“要不要做基本面表”，而是：

### 第一优先：从“首版落库”转向“模型消费分层”

最合理的下一步不是继续空谈，而是直接进入：

**`fundamental_quarterly.csv` → `model_features` 的消费分层设计**

也就是明确：

- 哪些字段直接保留
- 哪些字段做派生计算
- 哪些字段暂时不用
- 哪些文本字段做标签化 / 解释层使用
- 哪些口径要统一修正

### 第二优先：整理字段映射与特征层

下次主线应优先产出一份类似：

**《fundamental_quarterly → model_features 字段映射表 V1》**

目标是把当前主表从“可读表”转成“可训练上游表”。

### 第三优先：未来再扩到标准财报底表

后续再考虑：

- 补 6 年标准财务报表
- 建标准报表底表
- 建文档解析层
- 扩展 Excel / PDF / Word ingestion

---

## 九、下次对话直接执行口令

下次进入这个仓库主线后，直接按下面口令继续：

**继续 JD-data 主线，不回退旧话题，直接从“基本面首版落库”转入“模型消费分层”阶段。先围绕 `fundamental_quarterly.csv` 制定 `fundamental_quarterly -> model_features` 字段映射表 V1，明确哪些字段直接保留、哪些做派生、哪些暂时弃用、哪些文本字段进入解释层。**

如果该映射表完成，再继续：

1. 定义首版 `fundamental_features` 表结构  
2. 确定口径统一规则，优先处理 `external_customer_revenue` 等歧义字段  
3. 规划后续标准财报底表与 Excel / PDF / Word 预处理 ingestion 层

---

## 十、本文件用途

本文件是 2026-03-20 当前轮次开发结束前的主线锚点文档，作用是：

- 防止下次开机后开发跑偏
- 明确当前已经不是 schema 讨论阶段
- 明确当前 2021–2025 基本面首版骨架已完成
- 明确当前 `fundamental_quarterly.csv` 已可作为模型上游输入表
- 明确下一步主线不是继续补历史年，而是转向模型消费分层设计

---

## 十一、一句话状态结论

当前 JD-data 已完成 **2021–2025 京东物流基本面首版骨架落库**，`fundamental_quarterly.csv` 已经可以作为后期模型的上游输入表，但不能裸喂；下次主线应直接转入 **`fundamental_quarterly -> model_features` 的消费分层设计**。
