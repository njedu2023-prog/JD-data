# 0318 衔接口令

## 一、当前主线位置

当前仓库主线已从“日频行情原始数据 / 清洗数据接入”推进到 **京东物流基本面数据底座设计**。

本轮已完成的核心成果：

1. 已明确：`JD-data` 不仅要有价格层 `data_raw / data_clean`，还要新增 **基本面层**，为后续京东物流预测模型提供中长期经营数据支撑。
2. 已形成文档：**《京东物流基本面主表 schema V1》.MD**
3. 该 schema 已完成以下内容：
   - 文档定位
   - 建议落地路径
   - 主表粒度定义
   - 主键定义
   - Schema V1 字段总表
   - 缺失值策略
   - 推荐公式口径

---

## 二、当前已确认的核心结论

### 1. 企业基本面不是一张“随便手写的表”
它本质上是一个 **按财报周期更新的结构化主表**，服务于：

- 未来 1 个月预测
- 未来 6 个月预测
- 财报周期状态识别
- 事件后趋势判断
- 经营质量变化跟踪

### 2. 基本面表不是交易日频表
它 **不替代** 现有：

- `data_raw`
- `data_clean`

而是新增一层：

- `data_fundamental`

### 3. 当前建议落地路径已确认
建议文件路径：

`data_fundamental/02618.HK/fundamental_quarterly.csv`

### 4. 当前主表粒度已确认
一行 = 一个财报 / 经营披露周期

兼容周期：

- `quarter`
- `semiannual`
- `annual`

主键建议：

`symbol + report_date + period_type`

---

## 三、《京东物流基本面主表 schema V1》当前字段框架

当前 V1 已按 10 大类完成字段设计：

### A. 主键与身份字段
### B. 收入与增长字段
### C. 利润与盈利能力字段
### D. 现金流与资本开支字段
### E. 资产负债结构字段
### F. 运营效率字段
### G. 业务结构与经营规模字段
### H. 市场与估值辅助字段
### I. 事件与管理层备注字段
### J. 数据治理字段

并已明确规则：

- 英文字段后跟中文注释
- 关键字段不能只写在 note 里
- 未披露字段允许为空，但必须可追溯
- 必须保留 source / ingest / verify 质量链路
- 已定义主要比率口径公式

---

## 四、当前阶段性判断

截至 2026-03-18，本仓库的 **源数据底座与清洗后数据层** 已基本具备主框架。

更准确地说：

### 已经基本完成的部分
1. 日频行情原始层接入思路已形成
2. 清洗层目录与仓库级校验主线已基本建立
3. 仓库正在从“单条数据线接入”进入“系统化数据底座建设”阶段

### 还没有完成、但接下来必须补上的部分
1. 京东物流基本面层正式落库
2. 基本面字段来源映射
3. 基本面数据的自动 / 半自动 / 人工核验分层机制
4. 后续模型直接消费的统一数据接口

所以结论不是“全部完工”，而是：

**价格层底座已接近阶段收口，基本面层刚完成 schema 设计，接下来进入正式落库与数据来源映射阶段。**

---

## 五、下次开机后的开发主线

下次进入开发，不再重复讨论抽象概念，直接按下面顺序推进：

### 第一步：输出 `fundamental_quarterly.csv` 首版表头模板
目标：
把 schema 文档推进成真正可落库的 CSV 表头模板。

要求：
- 保持“英文字段 + 中文注释”
- 与《京东物流基本面主表 schema V1》一致
- 可作为后续真实落库模板使用

### 第二步：输出“字段来源映射表”
目标：
给基本面主表每个核心字段定义数据来源。

来源至少分为：

- 港交所公告
- 年报 / 中报 / 季报
- 财报附注
- 管理层讨论与分析
- 行情估值侧推算
- 人工补录
- 人工核验

### 第三步：区分三类字段
必须把字段分成三类：

#### A. 可直接结构化抓取
例如：
- revenue
- net_profit
- total_assets
- total_liabilities

#### B. 可半自动抽取
例如：
- management_guidance
- major_event_note
- revenue_growth_quality_note

#### C. 需人工核验 / 人工录入
例如：
- quality_note
- verified_by
- verified_time
- 某些经营口径备注字段

### 第四步：再进入 ingestion 设计
目标：
为后续真正写脚本做准备，形成：

- 数据源头定义
- 更新频率
- 文件落地规则
- 缺失值处理
- 质量闸门
- 人工核验链路

---

## 六、下次对话的直接执行口令

下次进入这个仓库主线后，直接按以下口令继续：

**继续 JD-data 主线，不回退旧话题，直接从《京东物流基本面主表 schema V1》往下推进：先输出 `fundamental_quarterly.csv` 首版表头模板，再做字段来源映射表。**

---

## 七、本文件用途

本文件是 2026-03-18 关机前的主线锚点文档，作用是：

1. 防止下次开机后开发跑偏
2. 明确当前不是继续补日频枝节，而是进入 **京东物流基本面层**
3. 作为后续 schema → CSV 模板 → 来源映射 → ingestion 设计 的衔接锚点

---

## 八、一句话状态结论

**当前 JD-data 已从“行情数据仓库建设”正式推进到“京东物流预测系统基本面底座建设”，下一步不是再谈概念，而是把 schema 落成可入库模板与字段来源映射。**
