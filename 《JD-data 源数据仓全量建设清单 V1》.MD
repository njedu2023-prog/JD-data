# JD-data 源数据仓全量建设清单 V1

## 一、文档定位

本文档不是模型文档，不是预测文档，也不是下游训练文档。

本文档的唯一目标是：

**在进入特征、标签、训练、回测、预测之前，先把 JD-data 所需的源数据仓底座一次性规划清楚，并按优先级完成建设，避免后续开发反复回头补基础数据。**

当前阶段主线只有一条：

**先把源数据仓收口。**

---

## 二、当前主线目标

当前 JD-data 的核心任务，不是“已经能抓到一点数据”，而是：

**把京东物流（02618.HK）的源数据，建设为可训练、可回放、可审计、字段完整、结构稳定、可持续扩展的正式单票数据仓。**

当前阶段明确不做以下事情：

- 不切模型主线
- 不讨论预测精度
- 不讨论策略收益
- 不讨论交易执行
- 不在脏表基础上继续缝补
- 不把 raw 与 clean 再混成一张表

---

## 三、当前已完成情况

截至当前版本，JD-data 已完成的基础工作如下：

### 1. 仓库与自动化基础已打通

已具备：

- GitHub 仓库
- GitHub Actions 自动运行能力
- TUSHARE_TOKEN Secrets 配置
- 日频刷新链路基本打通

### 2. 单票日线 raw / clean 分层已落地

当前仓库中已形成以下核心产物：

- `data_raw/02618.HK/hk_daily_raw.csv`
- `data_clean/02618.HK/daily_clean.csv`
- `jd-logistics-latest.json`
- `refresh_log/refresh_log.csv`

### 3. clean 主表空身份字段问题已被识别并修复

此前 `daily_clean.csv` 曾出现以下关键字段为空：

- `symbol`
- `market`
- `asset_type`

该问题已被识别为 clean 主表未收口的硬伤。  
现阶段已通过脚本修正与 Actions 验收，确认这三个身份字段已稳定写入，不再为空。

---

## 四、当前正式结论

### 结论 1：当前 raw / clean 分层方向正确

必须长期坚持：

- raw 层保留原始口径
- clean 层输出统一标准主表
- 下游只信任 clean
- raw 只用于追溯、重建与审计

### 结论 2：当前 clean 主表第一阶段已基本收口

当前 `data_clean/02618.HK/daily_clean.csv` 已具备：

- 身份字段非空
- 日期格式统一
- 字段命名统一
- 上游原始字段映射完成
- 基础质量闸门已建立

### 结论 3：当前数据对“工程收口”基本够用，但对“后续建模”仍明显不够

当前仅有：

- 单票
- 日线
- 上市以来约千余行历史

这对“源数据仓第一阶段收口”足够，  
但对后续高质量特征工程、标签设计、训练与预测来说，仍不够充分。

因此，下一步不应切到模型，而应继续建设系统未来真正需要的全量源数据底座。

---

## 五、当前阶段的最高原则

当前阶段的最高原则不是快，而是：

**一次性把未来系统真正需要的源数据底座规划清楚、按优先级拿到手，避免后续开发反复回头补基础数据。**

原因如下：

### 1. 若底层数据不齐，后续所有环节都会反复返工

例如：

- 特征做到一半，发现缺市场基准
- 标签设计好了，发现缺交易日历
- 训练开始后，才发现基础字段不够
- 回测报告出来后，才发现底层口径不一致

### 2. 源数据仓是整个系统的地基

后续无论是：

- 特征层
- 标签层
- 训练层
- 回测层
- 预测层

都必须建立在一套稳定、可追溯、字段长期锁定的源数据仓之上。

---

## 六、JD-data 当前与未来应建设的数据总表

本节用于明确：  
JD-data 未来不是只要一张 `daily_clean.csv`，而是要逐步建设成一个正式的源数据仓底座。

### A 类：第一优先级，模型前必须完成

#### A1. 个股 raw 日线表

路径：

- `data_raw/02618.HK/hk_daily_raw.csv`

作用：

- 保留 Tushare 原始字段
- 作为 clean 重建源
- 作为追溯依据
- 作为审计依据

要求：

- 每次全量拉取上市以来历史
- 字段口径保持上游原样
- 不得与 clean 混表
- 不得写入训练口径字段

当前状态：

- 已落地
- 需持续保持稳定

---

#### A2. 个股 clean 日线主表

路径：

- `data_clean/02618.HK/daily_clean.csv`

作用：

- 作为下游唯一可信正式主表
- 后续特征、标签、训练统一从此读取

字段口径：

- `symbol`
- `market`
- `asset_type`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `prev_close`
- `volume`
- `amount`
- `pct_change`
- `ret_1d`
- `log_ret_1d`
- `quality_flag`
- `ingest_time`
- `data_version`

要求：

- 身份字段不得为空
- OHLCV 不得为空
- 日期格式统一
- 不得重复日期
- 日期升序
- 不允许 FAIL 行静默落盘

当前状态：

- 已落地
- 第一阶段基本收口
- 后续需长期稳定维护

---

#### A3. latest 快照

路径：

- `jd-logistics-latest.json`

作用：

- 提供最近一日标准化快照
- 供轻量消费端读取
- 作为 clean 主表的最新行镜像

要求：

- 必须从 clean 输出
- 不允许直接从 raw 输出
- 口径必须与 clean 保持一致

当前状态：

- 已落地
- 后续继续保留

---

#### A4. refresh 日志表

路径：

- `refresh_log/refresh_log.csv`

作用：

- 记录每次刷新情况
- 便于失败追踪
- 便于审计与回放

建议字段：

- `refresh_time`
- `source`
- `symbol`
- `rows_raw`
- `rows_clean`
- `rows_fail`
- `status`
- `message`

要求：

- 每次刷新都必须记录
- 成功与失败都应留痕
- 不能只记录成功

当前状态：

- 已落地
- 需继续保留并稳定追加

---

### B 类：第二优先级，应尽快进入建设

#### B1. 港股交易日历表

建议路径：

- `calendar/hk_trade_calendar.csv`

作用：

- 定义哪些日子是交易日
- 区分周末、节假日、非交易日
- 为后续 T+1 / T+2 逻辑提供底层依据
- 为缺数据诊断提供依据

为什么必须要有：

如果没有交易日历，后续很多判断会混乱：

- 某天没数据，是休市还是抓取失败
- T+1 是自然日还是交易日
- 回测窗口如何滚动
- 标签如何对齐

建议字段：

- `calendar_date`
- `is_trade_day`
- `market`
- `exchange`
- `note`

验收标准：

- 历史交易日完整
- 日期格式统一
- 能明确区分交易日与非交易日

当前状态：

- 尚未建设
- 建议列为下一阶段第一项

---

#### B2. 恒生指数日线表

建议路径：

- `data_raw/HSI/hsi_raw.csv`
- `data_clean/HSI/hsi_clean.csv`

作用：

- 提供港股市场总体基准
- 为后续超额收益、相对强弱、市场 regime 判断提供依据

为什么需要：

单票日线不能解释全部价格行为。  
后续若没有市场基准，很多个股波动无法区分到底是：

- 个股自身驱动
- 还是市场整体波动带来的结果

当前状态：

- 尚未建设
- 建议列为第二阶段核心任务

---

#### B3. 恒生科技指数日线表

建议路径：

- `data_raw/HSTECH/hstech_raw.csv`
- `data_clean/HSTECH/hstech_clean.csv`

作用：

- 提供科技成长风格基准
- 便于衡量京东物流与科技成长板块的联动性

当前状态：

- 尚未建设
- 建议与恒生指数同步规划

---

### C 类：第三优先级，底座稳定后逐步建设

#### C1. clean 扩展衍生字段层

说明：

当前 `daily_clean.csv` 以基础主表为主。  
未来可在不污染 raw 的前提下，增加扩展字段层。

建议后续预留字段：

- `amplitude`
- `hl_spread`
- `oc_spread`
- `gap_open`
- `turnover_proxy`
- `rolling_ret_5d`
- `rolling_ret_20d`
- `rolling_vol_20d`
- `amount_ma_5`
- `amount_ma_20`
- `volume_ma_5`
- `volume_ma_20`

注意：

这些字段未来可以做成独立扩展层，  
不建议在当前阶段把 clean 主表堆得过重。

当前状态：

- 暂未进入正式建设
- 先记入建设清单，不作为当前第一优先级

---

#### C2. 行业 / 市场联动层

未来可考虑补充：

- 行业指数
- 物流相关指数
- 同类港股参考标的
- 市场风险代理因子

作用：

- 让后续特征层不只依赖单票时间序列
- 提升模型对市场状态的理解能力

当前状态：

- 暂缓
- 待底座稳定后再纳入

---

## 七、当前阶段必须锁死的结构原则

### 原则 1：raw / clean 必须长期分层

不得：

- 把 raw 与 clean 混成一张表
- 在 raw 中加入训练口径字段
- 在 clean 中残留上游原始脏口径

### 原则 2：clean 是下游唯一可信主表

后续：

- 特征层只读 clean
- 标签层只读 clean
- 训练层只读 clean

不能让下游直接绕过 clean 去吃 raw。

### 原则 3：latest 必须来自 clean

不能从半成品、混表、原始表直接导出 latest。

### 原则 4：失败不得静默覆盖旧 clean

如果当次刷新失败：

- 可以写日志
- 可以报错中断
- 但不能把脏 clean 覆盖到正式主表上

### 原则 5：字段契约一旦锁定，不得随意漂移

尤其是：

- 字段名
- 日期格式
- 单位口径
- 输出路径
- latest 结构

这些都必须尽量稳定。

---

## 八、当前阶段正式验收标准

以下验收标准用于判断 JD-data 当前阶段是否真正收口。

### 1. A1 个股 raw 日线表验收

- 能从上游全量拉取上市以来历史
- raw 文件成功落库
- 字段保持上游原始口径
- 日期无明显断档
- 无重复主键行

### 2. A2 个股 clean 日线主表验收

- `symbol / market / asset_type` 非空
- `trade_date` 非空
- `open / high / low / close / prev_close / volume / amount` 非空
- `trade_date` 无重复
- `trade_date` 升序
- `quality_flag` 不允许存在 FAIL 行落盘
- latest 可由 clean 正常导出

### 3. A3 latest 验收

- latest 可正常输出
- latest 字段与 clean 最新行一致
- latest 不允许与 clean 口径脱节

### 4. A4 refresh_log 验收

- 每次刷新均有记录
- 成功和失败都留痕
- message 能表达主要状态

### 5. 自动化验收

建议 workflow 后续加入自动闸门，至少检查：

- 身份字段是否为空
- 关键行情字段是否为空
- `trade_date` 是否重复
- `trade_date` 是否升序

---

## 九、下一阶段正式优先级

当前阶段后续工作，不再模糊推进，直接按以下优先级执行。

### 第一优先级

**建设港股交易日历表**

原因：

- 它是后续所有 T+1 / T+2、缺失诊断、标签对齐的底层依据
- 没有交易日历，后续很多逻辑都会埋雷

### 第二优先级

**建设恒生指数 raw / clean 日线表**

原因：

- 为单票提供市场基准
- 后续相对强弱与市场状态判断离不开它

### 第三优先级

**建设恒生科技指数 raw / clean 日线表**

原因：

- 提供风格基准
- 增强对科技成长环境的刻画能力

### 第四优先级

**补齐 workflow 自动验收闸门**

原因：

- 当前已有脚本校验
- 但还应把验收标准明确前置到自动流程中

### 第五优先级

**更新 README / schema 文档与实际落地保持一致**

当前已有部分文档与实际产物不完全一致，例如：

- README 中部分路径与现状不一致
- 实际 clean 为 csv，不是 parquet
- latest 实际位于根目录

这些都应尽快同步，避免文档与实际仓库状态脱节。

---

## 十、当前阶段禁止跑偏项

在本阶段开发中，必须避免以下行为：

### 1. 禁止切到模型主线

现在还没到模型阶段。  
源数据仓未完全收口前，不进入训练与预测主线。

### 2. 禁止讨论预测精度

当前重点不是“准不准”，而是“底座稳不稳”。

### 3. 禁止在脏表基础上继续缝补

如果结构不对，就全量重建，不能在脏表上继续追加补丁。

### 4. 禁止 raw / clean 混表

此条必须长期锁死。

### 5. 禁止文档与真实仓库状态长期脱节

文档必须逐步同步到真实产物、真实路径、真实字段。

---

## 十一、当前阶段一句话总结

当前 JD-data 的真正主线不是“抓到一点数据”，而是：

**把京东物流及后续相关底层市场数据，建设成一套可训练、可回放、可审计、字段完整、结构稳定、长期可扩展的正式源数据仓。**

---

## 十二、执行口令

当前阶段后续开发，统一按以下口令推进：

**继续 JD-data 主线开发。当前重点不是模型，而是把系统未来真正需要的全量源数据底座先建设到手。先完成交易日历、市场基准、自动验收闸门，再进入下游特征与训练层。不要切模型主线，不要在脏表上缝补，先把源数据仓彻底收口。**
