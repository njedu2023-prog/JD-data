# 0316 衔接口令

## 一、当前项目与主线定位

当前项目仓库：

- `JD-data`

当前主线目标：

- 把 `JD-data` 从“能抓到一点港股日线数据的初级仓库”，升级为**可训练、可回放、可审计、可扩展的单票正式源数据仓**
- 当前首个标的：
  - **京东物流（02618.HK）**

当前阶段，不讨论模型，不讨论预测精度，不切到下游训练层。  
**主线唯一重点：先把源数据仓彻底收口。**

---

## 二、当前已经完成的事情

### 1. 文档侧
开发前规范类文档已经开始建设，包括：

- 单票量化预测系统需求规范
- 单票数据仓建设规范
- Roadmap / Checklist 等

这些文档的作用是：

- 锁定主线
- 防止工程漂移
- 为后续数据层、特征层、标签层、训练层提供上位约束

### 2. GitHub Actions 已打通
当前 `JD-data` 已经具备：

- GitHub Actions 定时/手动运行能力
- `TUSHARE_TOKEN` 已放入 GitHub Secrets
- 能自动调用 Tushare 拉取港股日线

### 3. 数据已开始落库
当前仓库里已经出现：

- `data_raw/02618.HK/history.csv`
- `data_clean/02618.HK/daily_clean.csv`
- `jd-logistics-latest.json`
- `refresh_log/refresh_log.csv`

说明：

- 源数据链路已跑通
- 但数据层**还没有正式收口**

---

## 三、当前已确认的问题

这是下一轮必须接着处理的重点。

### 1. 历史数据量曾经明显不够
最初 `history.csv` 只有 2026-01 到 2026-03 的少量交易日，样本远远不够。

后续已经通过全量拉取改善，但仍需持续确认：

- 是否已覆盖上市以来全历史
- 是否无断档
- 是否无脏行
- 是否可稳定每日刷新

### 2. `history.csv` 曾经混过两套 schema
之前出现过：

- 一部分行是标准字段口径
- 一部分行是 Tushare 原始字段口径
- 日期格式混乱：
  - `YYYY-MM-DD`
  - `YYYYMMDD`

这个问题已经被识别为硬伤。  
后续必须坚持：

- raw 和 clean 分层
- 不允许再混表

### 3. `daily_clean.csv` 仍出现空字段
已明确发现：

- `symbol`
- `market`
- `asset_type`

在 `daily_clean.csv` 中出现空值。

这是当前最关键的未收口问题之一。

其性质不是小问题，而是：

- clean 主表身份字段失效
- 主表还不能视为正式训练主表
- 后续特征/标签/训练都不能放心接

### 4. 当前脚本与 workflow 仍属于“可跑但不够正式”的状态
已经明确：

- 旧版脚本仍存在增量逻辑
- 旧版 workflow 较简陋
- 提交信息写死
- 缺少并发控制
- 缺少 clean 主表非空校验闸门

---

## 四、当前已明确的正确工程方向

这是后续不能偏离的正式方向。

### 1. 每次全量拉取上市以来历史
当前阶段不再走“按 max(trade_date) 增量修补”的主线。

统一原则：

- 每次从京东物流上市后起始日全量拉取
- 通过全量重建保证历史纠偏能力
- 避免旧脏数据永久留在仓库

### 2. raw / clean 分层
必须长期坚持：

#### raw 层
保留 Tushare 原始字段：

- `ts_code`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `pre_close`
- `vol`
- `amount`

#### clean 层
输出标准训练主表字段：

- `symbol`
- `market`
- `asset_type`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `prev_close`
- `volume`
- `amount`
- `pct_change`
- `ret_1d`
- `log_ret_1d`
- `quality_flag`
- `ingest_time`
- `data_version`

### 3. clean 表必须加硬校验闸门
后续 `daily_clean.csv` 不允许再“看起来生成成功，但身份字段是空的”。

必须加校验：

- `symbol` 非空
- `market` 非空
- `asset_type` 非空
- `trade_date` 非空
- `open/high/low/close/prev_close/volume/amount` 非空
- 不允许 FAIL 行静默落盘
- 不允许 duplicated trade_date

### 4. latest 必须从 clean 输出
不再从半成品或混表输出 latest。  
必须保持：

- latest 与 clean 主表口径一致

### 5. refresh_log 必须继续保留
刷新日志要持续记录：

- refresh_time
- source
- symbol
- rows_raw
- rows_clean
- rows_fail
- status
- message

---

## 五、当前建议采用的正式文件结构

在 `JD-data` 中，当前阶段建议固定为：

- `data_raw/02618.HK/hk_daily_raw.csv`
- `data_clean/02618.HK/daily_clean.csv`
- `jd-logistics-latest.json`
- `refresh_log/refresh_log.csv`
- `scripts/fetch_hk_daily.py`
- `.github/workflows/fetch_hk_daily.yml`

---

## 六、下一轮必须优先处理的任务

优先级从高到低如下。

### 第一优先级：修复 clean 主表空字段问题
目标：

- `daily_clean.csv` 中
  - `symbol`
  - `market`
  - `asset_type`
  全列不得为空

验收标准：

- 任意抽查多行，身份字段均有值
- workflow 成功后 clean 主表无空身份字段

### 第二优先级：脚本正式收口
脚本必须改成：

- 全量拉取上市以来历史
- raw / clean 分层
- 统一日期格式
- 统一字段命名
- 严格非空校验
- 校验失败直接中断

### 第三优先级：workflow 正式收口
workflow 必须升级为：

- 有并发控制
- 有更合理的 commit message
- 只在文件有变动时提交
- 保持每日收盘后自动刷新

### 第四优先级：确认历史完整性
确认 `daily_clean.csv` 是否已经覆盖：

- 京东物流上市以来全历史
- 无明显断档
- 无重复日期
- 无错序

---

## 七、下一轮之后的第二阶段方向

只有在当前主表彻底收口后，才进入下面这些任务：

### 1. 增加基础衍生字段
例如：

- `amplitude`
- `hl_spread`
- `oc_spread`

### 2. 增加交易日历层
建立：

- 港股交易日历表
- 非交易日 / 停牌 / 缺失原因区分逻辑

### 3. 增加市场联动输入
逐步接入：

- 恒生指数
- 恒生科技指数
- 相关市场风险代理因子

### 4. 为下游特征层、标签层做正式接口
即：

- clean 主表成为下游唯一可信主表
- 后续特征和标签统一从 clean 层读取

---

## 八、当前阶段禁止跑偏的点

下一轮开发中，必须避免以下错误：

### 1. 不要切去模型层
当前还没到模型主线。

### 2. 不要讨论“预测准不准”
当前核心问题仍是源数据 clean 主表工程完整性。

### 3. 不要继续在混乱表上追加修补
如果结构不对，应该全量重建，而不是在脏表上继续缝补。

### 4. 不要把 raw 和 clean 再混在一张表里
这条必须长期锁死。

---

## 九、当前阶段一句话总结

当前 `JD-data` 的真正主线不是“抓到数据”，而是：

**把抓到的数据，收口成可训练、可回放、可审计、字段完整、结构稳定的正式单票 clean 主表。**

---

## 十、下一轮对话可直接接续的口令

可直接用下面这段作为下一轮对话起点：

> 继续 `JD-data` 主线开发。当前重点不是模型，而是收口源数据仓。  
> 已确认问题：`daily_clean.csv` 仍有空身份字段（symbol/market/asset_type），说明 clean 主表还不合格。  
> 下一步优先做三件事：  
> 1）整体覆盖 `scripts/fetch_hk_daily.py`，改成上市以来全量拉取 + raw/clean 分层 + 非空校验；  
> 2）整体覆盖 `.github/workflows/fetch_hk_daily.yml`，加入并发控制与更合理提交流程；  
> 3）跑一次 Actions 后验收 `data_clean/02618.HK/daily_clean.csv`，确认身份字段不再为空。  
> 不要切模型主线，先把 clean 主表彻底收口。
