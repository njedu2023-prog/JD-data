# 0316 衔接口令

## 一、当前项目与主线定位

当前项目仓库：

`JD-data`

当前主线目标：

把 JD-data 从“能抓到一点港股日线数据的初级仓库”，升级为 **可训练、可回放、可审计、可扩展、可整仓刷新、可整仓验收** 的正式源数据仓底座。

当前阶段首个核心标的：

- 京东物流 `02618.HK`

当前已纳入的底层市场基准与代理层包括：

- 港股交易日历
- 恒生指数 `HSI`
- 恒生科技指数 `HKTECH`
- 恒生中国企业指数 `HSCEI`
- 京东集团代理层 `9618.HK`
- 美团代理层 `3690.HK`

当前阶段明确不做：

- 不切模型主线
- 不讨论预测精度
- 不讨论交易收益
- 不进入下游训练层
- 不在脏表上继续缝补
- 不把 raw 和 clean 混成一张表

当前主线唯一重点：

**继续补齐未来系统真正需要的市场联动 / 基准 / 代理层底表，并保持整仓刷新与整仓验收总线持续有效。**

---

## 二、当前已经完成的关键成果

### 1. 京东物流单票 raw / clean 分层已打通

当前已稳定落地：

- `data_raw/02618.HK/hk_daily_raw.csv`
- `data_clean/02618.HK/daily_clean.csv`
- `jd-logistics-latest.json`

并已确认：

- `symbol / market / asset_type` 不为空
- `daily_clean.csv` 已作为正式 clean 主表可用
- `latest` 从 clean 输出，不再走半成品口径

---

### 2. 京东集团代理层 raw / clean 已打通

当前已稳定落地：

- `data_raw/9618.HK/hk_daily_raw.csv`
- `data_clean/9618.HK/daily_clean.csv`

并已确认：

- `symbol = 9618.HK`
- `market = HK`
- `asset_type = equity`
- `trade_date` 升序
- `quality_flag = PASS`
- 单线 workflow 已跑通
- 已接入整仓总闸门与整仓总线

---

### 3. 美团代理层 raw / clean 已打通

当前已稳定落地：

- `data_raw/3690.HK/hk_daily_raw.csv`
- `data_clean/3690.HK/daily_clean.csv`

并已确认：

- `symbol = 3690.HK`
- `market = HK`
- `asset_type = equity`
- `trade_date` 升序
- `quality_flag = PASS`
- 单线 workflow 已跑通
- 已纳入整仓正式成员范围

---

### 4. 港股交易日历表已落地

当前已落地：

- `calendar/hk_trade_calendar.csv`

已确认：

- 字段完整
- 日期升序
- `is_trade_day` 仅为 `0 / 1`
- `market = HK`
- `exchange = XHKG`

这张表已可作为后续 T+1 / T+2、缺失诊断、标签对齐的底层时间基准。

---

### 5. 恒生指数 raw / clean 已落地

当前已落地：

- `data_raw/HSI/hsi_raw.csv`
- `data_clean/HSI/hsi_clean.csv`

已确认：

- `symbol = HSI`
- `market = HK`
- `asset_type = index`
- 日期升序
- `quality_flag = PASS`
- 指数 raw 中 `vol / amount` 缺失可接受
- clean 层已统一补齐 `volume / amount`

---

### 6. 恒生科技指数 raw / clean 已落地

当前已落地：

- `data_raw/HKTECH/hktech_raw.csv`
- `data_clean/HKTECH/hktech_clean.csv`

已确认：

- `symbol = HKTECH`
- `market = HK`
- `asset_type = index`
- 日期升序
- `quality_flag = PASS`
- 指数 raw 中 `vol / amount` 缺失可接受
- clean 层已统一补齐 `volume / amount`

---

### 7. 恒生中国企业指数 raw / clean 已落地

当前已落地：

- `data_raw/HSCEI/hscei_raw.csv`
- `data_clean/HSCEI/hscei_clean.csv`

已确认：

- `symbol = HSCEI`
- `market = HK`
- `asset_type = index`
- 日期升序
- `quality_flag = PASS`
- 通过 `yfinance ^HSCE` 接入
- 已接入整仓总闸门与整仓总线

---

### 8. refresh_log 已统一收口

当前已确认：

`refresh_log/refresh_log.csv` 已从旧脏格式清洗为统一 schema，不再混用两套列名，已能被统一验收脚本正常读取。

当前统一字段为：

- `refresh_time`
- `source`
- `symbol`
- `rows_raw`
- `rows_clean`
- `rows_fail`
- `status`
- `message`

当前已可稳定记录以下 symbol：

- `02618.HK`
- `9618.HK`
- `3690.HK`
- `HK_CALENDAR`
- `HSI`
- `HKTECH`
- `HSCEI`

---

### 9. README 与真实仓库目录已同步

当前 README 已反映真实仓库状态，已同步展示：

- 京东物流 raw / clean
- 京东集团 raw / clean
- 美团 raw / clean
- 港股交易日历
- HSI raw / clean
- HKTECH raw / clean
- HSCEI raw / clean
- scripts
- workflows
- refresh_log
- latest

---

### 10. 仓库级统一验收脚本已建立并扩展

当前已新增并跑通：

- `scripts/validate_repository.py`
- `.github/workflows/validate_repository.yml`

该总闸门当前已可统一检查：

- 关键文件是否存在
- clean 主表是否非空
- 身份字段是否正确
- 日期是否重复
- 日期是否升序
- `quality_flag` 是否全 PASS
- `refresh_log` 是否结构正确、关键 symbol 是否齐全

并且已经实际覆盖：

- `02618.HK`
- `9618.HK`
- `3690.HK`
- `HK_CALENDAR`
- `HSI`
- `HKTECH`
- `HSCEI`

---

### 11. 一键整仓刷新 + 总验收总线已扩展并跑通

当前已新增并跑通：

- `.github/workflows/refresh_all_and_validate.yml`

该总线当前已实现：

- 刷新京东物流日线
- 刷新京东集团日线
- 刷新美团日线
- 刷新港股交易日历
- 刷新恒生指数
- 刷新恒生科技指数
- 刷新恒生中国企业指数
- 最后统一跑 `validate_repository.py`

并已成功跑通，说明：

**JD-data 已进入“整仓可刷新、整仓可验收、整仓可维护”的正式底座阶段。**

---

## 三、当前仓库已正式具备的结构能力

截至当前阶段，JD-data 已不再是“几条脚本单独能跑”的状态，而是已经具备以下三层能力：

### 1. 分层能力

- raw 层保存上游原始口径
- clean 层输出正式标准主表
- latest 从 clean 层导出
- raw 与 clean 长期分离

### 2. 多表能力

当前已不再只有一张单票表，而是已有：

- 京东物流单票主表
- 京东集团代理层主表
- 美团代理层主表
- 交易日历表
- 港股市场基准表
- 科技成长风格基准表
- 中资港股风险基准表
- 刷新日志表

### 3. 总线能力

当前已具备：

- 分线抓取 workflow
- 仓库级统一验收 workflow
- 一键整仓刷新 + 总验收 workflow

这意味着后续开发已经可以站在更正式的仓库级底座上推进，而不再是零散表格级别的推进。

---

## 四、当前阶段正式验收结论

当前阶段已完成的正式验收如下：

### 已通过

- 京东物流 clean 主表验收
- 京东集团 clean 主表验收
- 美团 clean 主表验收
- 港股交易日历验收
- 恒生指数 clean 主表验收
- 恒生科技指数 clean 主表验收
- 恒生中国企业指数 clean 主表验收
- refresh_log 结构验收
- README 与真实目录一致性验收
- `validate_repository.yml` 验收通过
- `refresh_all_and_validate.yml` 验收通过

### 当前阶段一句话结论

**JD-data 当前已经从“脚本可跑”升级到“仓库级整仓可刷新、整仓可验收”的正式底座状态。**

并且已进一步升级到：

**“京东物流 + 京东集团 + 美团 + 港股市场基准”的正式底层联动底座状态。**

---

## 五、当前阶段仍然没做的事

虽然当前底座已经明显成型，但当前还没有进入模型主线。

仍未进入的内容包括：

- 特征层
- 标签层
- 训练层
- 回测层
- 预测层

原因不是技术不能做，而是：

**当前策略仍然是先把未来真正需要的源数据底座尽量收齐，再进入下游。**

---

## 六、下一阶段最合理主线

当前阶段完成后，下一步最合理的方向，不是回头修旧问题，而是继续补齐“未来系统真正需要的底层表”。

### 下一阶段建议主线

#### 第一优先级：继续补市场联动 / 基准 / 代理层

优先纳入：

- 阿里巴巴 `9988.HK`

其意义：

- 补齐电商平台竞争与供给侧代理层
- 与京东集团、美团形成更完整的平台竞争骨架
- 为后续特征层提供更完整的市场联动输入

#### 第二优先级：继续强化仓库级治理

继续完善：

- workflow 输出与调试信息
- 仓库级校验范围
- schema / contract 文档
- refresh_log 进一步标准化

#### 第三优先级：底座稳定后再进入下游

只有在底座继续稳定后，才进入：

- 特征层
- 标签层
- 训练层
- 回测层

---

## 七、当前阶段禁止跑偏项

下一轮开发中，必须继续避免以下错误：

### 1. 不要切去模型层

当前还没到模型主线。

### 2. 不要讨论“预测准不准”

当前核心问题仍然是源数据底座是否完整、稳定、可审计。

### 3. 不要在脏表上追加修补

如果结构不对，应继续采取全量重建与统一校验，不在脏表上缝补。

### 4. 不要把 raw 和 clean 再混回一张表

这条必须长期锁死。

### 5. 不要让文档再和真实仓库状态脱节

README、建设清单、workflow、脚本、真实目录必须继续同步。

---

## 八、当前阶段一句话总结

当前 JD-data 的真正主线不是“抓到一点数据”，而是：

**把京东物流、京东集团、美团及其相关市场基准数据，建设成一套可训练、可回放、可审计、字段完整、结构稳定、可整仓刷新、可整仓验收的正式源数据仓底座。**

---

## 九、下一轮对话可直接接续的口令

可直接用下面这段作为下一轮对话起点：

继续 JD-data 主线开发。当前重点不是模型，而是继续补齐未来系统真正需要的底层源数据表。

当前已完成并跑通的内容包括：

1）京东物流 raw / clean 主表；  
2）京东集团 9618.HK raw / clean；  
3）美团 3690.HK raw / clean；  
4）港股交易日历；  
5）恒生指数 HSI raw / clean；  
6）恒生科技指数 HKTECH raw / clean；  
7）恒生中国企业指数 HSCEI raw / clean；  
8）refresh_log 统一收口；  
9）README 与真实目录同步；  
10）validate_repository.yml 仓库级总闸门跑通；  
11）refresh_all_and_validate.yml 一键整仓刷新 + 总验收总线跑通。

当前结论：

JD-data 已从“脚本可跑”升级到“整仓可刷新、整仓可验收、整仓可维护”的正式底座阶段，并已进入“京东物流 + 京东集团 + 美团 + 港股市场基准”的底层联动阶段。

下一步不要切模型主线，优先继续补市场联动 / 基准 / 代理层。当前下一枪最合理的是：**阿里巴巴 9988.HK raw / clean 单线接入，然后再接入总闸门与总线。**
