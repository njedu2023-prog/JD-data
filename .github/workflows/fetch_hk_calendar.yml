name: fetch hk_calendar

on:
  workflow_dispatch:
  schedule:
    - cron: "20 19 * * *"

concurrency:
  group: jd-data-fetch-hk-calendar
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    env:
      TZ: Asia/Shanghai
      TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas tushare

      - name: Run hk calendar fetch script
        run: |
          python scripts/fetch_hk_calendar.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add calendar/hk_trade_calendar.csv refresh_log/refresh_log.csv || true

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "refresh hk_trade_calendar data"
          git push
